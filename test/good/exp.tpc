int a, b, c, d;
int a[2], b[1];
char a, b, d;

void do_something(void) {
    a = b;
}

int main(int a, char b) {

    if ((a < b) && (b < c)) {
        a = b + c;
    } else if (a != b) {
        b = a;
    }
}

int do_something_2(int c, int d) {
    printf(c + d);
}